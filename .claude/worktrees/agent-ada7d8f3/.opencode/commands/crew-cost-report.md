# Workflow Cost Report

Display a cost breakdown for the current or specified task.

## Command: /crew-cost-report $ARGUMENTS

Run `python3 /mnt/c/git/agentic-workflow/scripts/crew-cost-report.py $ARGUMENTS` and display the output to the user.

Use `--all` to show a summary across all tasks.
