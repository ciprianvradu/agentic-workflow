# Workflow Configuration

View current workflow configuration settings.

## Command: /crew-config

Run `python3 C:\git\agentic-workflow\scripts/crew-config.py` and display the output to the user.

If the user asks to change settings, edit `~/.claude/workflow-config.yaml` (or the project-level override) directly based on their request.
