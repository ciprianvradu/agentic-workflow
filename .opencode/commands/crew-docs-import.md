# Import Doc Templates

## Command: /crew-docs-import

Run `python3 C:\git\agentic-workflow\scripts/crew-docs-import.py --dry-run` first and show the user what would be created. Ask for confirmation before running without `--dry-run`.
